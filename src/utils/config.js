export const config = {
  url: "http://192.168.9.91:3000/" //todo url de test locale
  //a savoir que si on utilise localhost sur ios ok, sur android (mobile)
  //il va pointer sur le "localhost du mobile" et non de la machine, donc j'utilise l'ip de ma machine
}
